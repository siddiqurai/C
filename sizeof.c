#include <stdio.h>

int main()
{
    
    // Write a program that print sizeof value

    int i;
    float f;
    double d;
    char c;

    printf("Size of bytes = %d\n", sizeof(i));
    printf("Size of bytes = %d\n", sizeof(f));
    printf("Size of bytes = %d\n", sizeof(d));
    printf("Size of bytes = %d\n", sizeof(c));

    return 0;
}